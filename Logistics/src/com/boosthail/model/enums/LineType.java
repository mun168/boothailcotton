package com.boosthail.model.enums;

/**
 * Enum representing the type of a line.
 * A line can hold loose packages (via pallets), cartons or be a special mixed type.
 */
public enum LineType {
    LOOSE, CARTON, MIXED
}